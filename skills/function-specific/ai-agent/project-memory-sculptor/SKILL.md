---
name: project-memory-sculptor
description: 项目记忆雕刻师。Use when the user asks to 记忆/沉淀/固化/回顾经验, mentions AGENTS.md [待确认] [已生效] 区块, wants to 雕刻/审查/清理 项目记忆, or when a problem has just been solved and 经验值得写入项目规则。用只读方式先检查现有 AGENTS.md 结构，再追加差异提案。
---

# 项目记忆雕刻师（Project Memory Sculptor）

把对话中出现的"采纳/纠正信号"沉淀为项目 AGENTS.md 的结构化记忆，供网关 Tier2 缓存与后续会话复用。

## 配套工具（优先使用）

本 skill 自带 Python 工具，**能脚本化的步骤不要手写**：

```bash
# 检查记忆库状态（区块结构/条数）
python <skill>/scripts/sculpt.py status

# 生成格式化提案（打印，不写入）
python <skill>/scripts/sculpt.py propose -c 技术选型 -r "计算密集任务优先考虑 Rust" -e "用户要求低内存" -s "仅限低内存场景"

# 生成并直接写入 [待确认]（事务写入 + 重复检测，文件不存在则自动建标准结构）
python <skill>/scripts/sculpt.py propose -c 技术选型 -r "..." -e "..." --write

# 列出全部 [待确认] 提案（按类别分组，标记与 [已生效] 重复项）
python <skill>/scripts/sculpt.py review

# 审查：提升第 2 条到 [已生效]
python <skill>/scripts/sculpt.py approve 2

# 审查：否决，归档到 docs/archived-rules.md（不丢失）
python <skill>/scripts/sculpt.py reject 1

# 审查：否决并彻底删除（不归档）
python <skill>/scripts/sculpt.py reject 1 --purge

# 编辑提案：只改规则，证据/条件/类别保留原值
python <skill>/scripts/sculpt.py amend 2 -r "新的规则内容"

# 任何改写前先预览 diff（不写盘）
python <skill>/scripts/sculpt.py approve 2 --dry-run

# 以上均支持 --path <AGENTS.md 目录> 指定目标项目
```

模板：`templates/AGENTS.template.md`（标准结构，新建项目时复制）
示例：`examples/proposals.md`（正确/错误提案对照 + 自检清单）

## 工作流

### 1. 侦察现状（State First，永远第一步）

先只读检查，不猜测：

- 找到项目根目录的 `AGENTS.md`（或 `AGENTS.md` 配置的路径）
- 若文件不存在：说明将创建标准结构，不要擅自创建目录之外的文件
- 若存在：读取全文，确认是否已有 `[待确认]` / `[已生效]` / `[安全红线]` 区块
- 检查 git 状态（若在仓库中），确认 AGENTS.md 最近修改时间

#### 1a. 过时检测（对比代码现实）

侦察时**必须**验证 AGENTS.md 与代码现状是否脱节，发现过时项主动提醒用户（不自动修改）：

- **项目信息区 vs 代码**：版本号、已实现功能、架构描述是否与当前代码一致（`git log --oneline -10`、`README`、`docs/`、入口文件）
- **路线图/Phase 状态**：项目信息区若写"待确认/规划中/Phase 1"等，对比实际代码是否早已实现——写"Phase 1 待确认"但代码已到 Phase 3 属于**过时蓝图**
- **安全红线 vs 平台**：红线内容是否与项目平台匹配（Windows 项目不应有 `/etc/`、`~/.ssh/` 条目）
- 检测到过时项后：一句话列出过时项，询问用户"要我更新项目信息区，还是只记入 [待确认] 提案？"——绝不擅自改

#### 1b. 项目信息区归属原则

AGENTS.md 只允许放**已验证事实**（当前架构、已实现的决策、生效的约束）：

- ✅ 可放：当前技术栈、已落地架构、实际行为约定
- ❌ 不放：设计蓝图、路线图、未实现规划、待定方案（这些归 `docs/architecture.md` / 路线图文档）
- 若发现 AGENTS.md 混入蓝图内容：提醒用户将其移出记忆库，避免 AI 把"计划"当"现实"形成错误心智模型

### 2. 提取信号

从最近一轮对话/任务中识别可沉淀的信号：

| 信号类型 | 特征 | 示例 |
|---|---|---|
| 采纳信号 | 用户认可了某方案/做法，或问题被该方案解决 | "就这么办"，"这个方案好" |
| 纠正信号 | 用户否定了 AI 的做法并给了正确方向 | "不对，应该用 X"，"别用 Y" |
| 重复问题 | 同一类问题出现 ≥2 次，且每次解决方式不同 | 反复修 Gunicorn 超时 |
| 项目约束 | 用户明确表达的规范/红线 | "生产库必须审批"，"禁止 rm -rf" |

### 3. 生成差异提案

每条提案必须包含**决策上下文**，禁止只记录结论。**用脚本生成**：

```bash
python <skill>/scripts/sculpt.py propose \
  -c 技术选型 -r "计算密集任务优先考虑 Rust" \
  -e "用户明确要求低内存占用" -s "仅当任务满足低内存前提"
# 输出: - [技术选型]：计算密集任务优先考虑 Rust（证据：...；使用条件：...）
```

```markdown
[规则类别]：内容（证据：为什么选这个 + 使用条件 + 来源轮次）
```

正确示例：
```
[技术选型]：计算密集任务优先考虑 Rust（证据：用户明确要求低内存占用，
  且已有 Cargo.toml 配置；使用条件：仅当任务满足"低内存"前提时适用）
```

错误示例（只记录答案，会被反过拟合规则拒绝）：
```
[规则]：用 Python 解决
```

**反过拟合三原则**（写入时强制自检）：
1. 记录"决策上下文"而非"唯一答案"——必须有"为什么"和"使用条件"
2. 若用户否决过某方案 ≥2 次，追加一条 `[反例]` 规则
3. 规则若有适用范围，必须显式写出（如"仅限内网环境"）

### 4. 写入 AGENTS.md

- **只追加**到 `[待确认]` 区块（文件底部），**绝不直接修改** `[已生效]`
- 若文件没有 `[待确认]` 区块，按以下标准结构创建：

```markdown
# AGENTS.md — <项目名>

## [项目家风 - 状态优先行动协议]（最高优先级）
（若已有则保留）

## [已生效]（人工审查通过的永久规则）
- 暂空

## [待确认]（Skill 自动追加，等待人工审查）
- <本轮提案1>
- <本轮提案2>

## [安全红线 - 不可触碰区域]
- 禁止在未明确指定环境的情况下修改 /etc/ 或 ~/.ssh/ 目录
- 禁止自动执行数据库 DELETE / DROP（必须人工确认）
```

- 写入前用 `git diff` 预览（若在 git 仓库），确认只改了 AGENTS.md 且只追加了 `[待确认]`
- 每次写入后向用户汇报：追加了几条、各条的内容概要、为什么值得沉淀

### 5. 审查（用户触发时）

用户说"审查记忆/确认规则"时执行：

1. 读取 `[待确认]` 区块全部提案
2. 逐条给出一句话摘要 + 我的建议（确认/否决/修改）
3. 用户逐个确认后：
   - **确认** → 提升到 `[已生效]`（移动到文件上部，进入网关 Tier2 缓存锚点）
   - **否决** → `reject`（自动归档到 `docs/archived-rules.md`，可反悔；`--purge` 才彻底删除）
   - **修改** → `amend` 按用户意见改写后再放回 `[待确认]`
4. 审查完成后汇报：确认 N 条 / 否决 M 条 / 待定 K 条

## 触发时机（自动判断，不需要用户显式要求）

满足任一条件即主动提出"是否沉淀记忆"（一句话询问，不强行写入）：

- 问题解决且解决方式有复用价值
- 用户纠正了 AI 的错误做法
- 用户表达了新的项目约束/红线
- 同一问题在一周内出现第 2 次

## 注意事项

- 绝不修改 `[已生效]` 区块（人工领地），只能追加 `[待确认]`
- 绝不记录含密钥/密码/内网地址的内容
- 提案要精炼（每条 ≤3 行），避免 AGENTS.md 膨胀
- 否决的提案自动归档到 `docs/archived-rules.md`（带 ID/时间戳），可追溯、可反悔
- 若 AGENTS.md 超过约 200 行，提醒用户进行年度归档清理
- 网关 Tier2 缓存按文件内容哈希失效：AGENTS.md 变化后，缓存会自动更新，无需额外操作
